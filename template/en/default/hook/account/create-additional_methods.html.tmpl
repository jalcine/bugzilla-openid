[%#
  # TODO: Add the needed link to handle OpenID registeration.
  #%]

[% IF Param('user_info_class').split(',').contains('OpenID') %]
Or, register with your OpenID account:
<a href="page.cgi?p=openid_continue.html&mode=register">
    <img src="extensions/OpenID/web/openid.png" style="margin-bottom: -8px" alt="Log-in with OpenID"> Log-in with OpenID
</a>
[% END %]
